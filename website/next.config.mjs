/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow existing JSX component files without .tsx extension
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
};

export default nextConfig;
